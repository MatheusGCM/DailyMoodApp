import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  box: {
    alignItems: 'baseline',
    flexDirection: 'row',
  },
  txt: {
    fontSize: 16,
    fontWeight: '400',
    lineHeight: 24,
    color: '#969696',
    marginStart: 3,
  },
});

export default styles;

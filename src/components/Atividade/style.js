import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  container: {
    flexDirection: 'row',
    width: '80%',
  },
  box: {
    flexDirection: 'row',
    alignItems: 'center',
  },
  flex: {
    flexDirection: 'row',
  },
  icon: {
    marginHorizontal: 8,
  },
  txtIcon: {
    fontSize: 12,
    color: '#000',
    fontWeight: '600',
    lineHeight: 18,
    marginStart: 8,
  },
});

export default styles;
